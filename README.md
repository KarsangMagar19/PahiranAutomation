# Selenium Framework (Python + PyTest)

## 🚀 Features
- Page Object Model (POM)
- Explicit waits
- Config via .env
- JSON test data
- HTML reporting
- Logging support

## ⚙️ Setup

```bash
git clone <repo>
cd selenium-framework
pip install -r requirements.txt